# Production env vars for the packaged Electron app.
# This file is committed and bundled — do NOT put secrets here.
# Next.js auto-loads this file when NODE_ENV=production.

# URL of the cloud app used by the Cloud Sync feature.
CLOUD_API_URL=https://fyp.jameswong.work
